project Queue_management is

       package Linker is
          for Default_Switches ("ada") use ("-g");
       end Linker;

       package Builder is
          for Default_Switches ("ada") use ("-g");
       end Builder;

       package Compiler is
          for Default_Switches ("ada") use ("-g");
       end Compiler;

       for Main use ("manager.adb", "bank_counter.adb");

end Queue_management;
